# Thiranex-Password-Strength-Analyzer
Python script created during my internship to analyse password robustness. Implements regular expressions for validation checks (length, digits, symbols) and uses cryptographically secure modules for automated alternative suggestions.
